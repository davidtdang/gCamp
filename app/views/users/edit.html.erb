<div class= "page-header">
  <h1>Edit User</h1>
</div>

<% @usersreturnpath = users_path %>

<%= render 'form' %>

<div class="form-group">
  <div class="actions">
    <div class="col-sm-offset-2 col-sm-10">
      <div class="well col-md-6">
        <center><%= link_to "Delete User", @user, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger" %></center>
      </div>
    </div>
  </div>
</div>
