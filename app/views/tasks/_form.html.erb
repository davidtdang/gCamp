<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation" class="alert alert-success" role="alert">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<!-- lines (68 sloc)  2.281 kb RawBlameHistory   
<%= form_for @person, html: {class: "form-horizontal"} do |f| %>
<% if @person.errors.any? %>
<div id="error_explanation" class="alert alert-danger col-sm-offset-2" role="alert">
  <h4><%= pluralize(@person.errors.count, "error") %> prohibited this person from being saved:</h4>

  <ul>
    <% @person.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %> -->
